#
# Common rules and values used by all xosview Makefiles.
#
# The configure script should set up this file for most all cases.
# One can override compile / link options here if needed.
#

# version number for release
VERS = @PACKAGE_VERSION@

TOP = @TOP_SRCDIR@

#-----------------------------------------------------------------
# C++ compiler
#-----------------------------------------------------------------
CXX           = @CXX@

#-----------------------------------------------------------------
# Compiler flags
#-----------------------------------------------------------------

# Optimized CXXFLAGS
CXXFLAGS      += $(CFLAGS) @EXTRA_CXXFLAGS@ @XINCS@ \
                 -I$(TOP) -I$(TOP)/@host_dir@ @DEFS@
# DEBUG CXXFLAGS
#CXXFLAGS      += $(CFLAGS) -Wall -ggdb -pipe @XINCS@ \
#                 -I$(TOP) -I$(TOP)/@host_dir@ @DEFS@
# PROFILE CXXFLAGS
#CXXFLAGS      += $(CFLAGS) -Wall -ggdb -pg -pipe @XINCS@ \
#                 -I$(TOP) -I$(TOP)/@host_dir@ @DEFS@

#-----------------------------------------------------------------
# Link flags and libraries
#-----------------------------------------------------------------

# NORMAL LIBS
LFLAGS        += -L$(TOP)/@host_dir@
LIBS          += -lmeter @EXTRALIBS@ @XLIBS@

# DEBUG LIBS
#LFLAGS        += -L$(TOP)/@host_dir@ -static
#LIBS          += -lmeter @EXTRALIBS@ @XLIBS@

# PROFILE LIBS
#LFLAGS        += -L$(TOP)/@host_dir@ -pg
#LIBS          += -lmeter @EXTRALIBS@ @XLIBS@ -lgmon

#-----------------------------------------------------------------
# Rules
#-----------------------------------------------------------------
##  The GNU-style rule of %.o: %.cc is not as portable as .cc.o.
.cc.o:
	$(CXX) $(CXXFLAGS) @CPP_DEP@ -c $< -o $@

@AUTODEPRULE@
