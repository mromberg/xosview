#
# GNU Makefile template
#
#
# $Id: Makefile.gnu.in,v 1.1 2008/02/28 23:46:57 romberg Exp $
#
include ../Makefile.config
@AUTODEPRULE_SUBDIR@

#arch=$(shell uname -m)
#export arch

CXXFLAGS += -I@TOP_SRCDIR@

CXXOBJS = \
memmeter.o \
swapmeter.o \
pagemeter.o \
loadmeter.o \
MeterMaker.o

OBJS = $(CXXOBJS) get_def_pager.o

ARCH = @host_cpu@

CFILES := $(CXXOBJS:.o=.cc) get_def_pager.c
DEPFILES := $(OBJS:%=.%.d)

vpath %.h $(TOP)
vpath %.cc $(TOP)

#-----------------------------------------------------------------------

all : libmeter.a

clean :
	rm -f $(OBJS) $(DEPFILES) libmeter.a *~

libmeter.a : $(OBJS)
	ar cvr libmeter.a $(OBJS)
	@RANLIB@ $@

@AUTODEPEND@
